#include<iostream>
#include<cstring>
using namespace std ; 
int main () 
{
    char mobile[15] ;
    int count = 0 ;
    cout << endl << "Enter your mobile number : " ;
    fflush(stdin);
    fgets(mobile,15,stdin);
    count = strlen(mobile) - 1 ;
    try
    {
        if(count < 10)
        throw 'a';
        if(count > 10)
        throw 1 ;
    }
    catch(char)
    {
        cout << endl << "ERROR : Invalid mobile number !!!" << endl ;      
        cout << "[ does not contain 10 digits ]" << endl << endl ;    
        exit(0);  
    }
    catch(int)
    {
        cout << endl << "ERROR : Invalid mobile number !!!" << endl ;      
        cout << "[ contains more than 10 digits ]" << endl << endl ;    
        exit(0);  
    }
    cout << endl << "Your mobile number is : " << mobile << endl ;
    return 0 ; 
}
