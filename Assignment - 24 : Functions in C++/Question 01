void prime(int n)
{
    int count = 0 ;
    for(int i = 2 ; i < n ; i++)
    {
        if(n%i == 0)
        {
            count++;
            break;
        }
    }
    if(count>0 || n<=0 || n==1)
    cout << "Not prime";
    else
    cout << "prime"; 
}
